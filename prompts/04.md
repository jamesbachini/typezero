## Task 4/5 — Backend API (testnet) + auto wallet generation + friendbot funding + prove endpoint

**You are Codex. Implement this task fully before moving on. Run the smoke tests.**

### Goal

Backend server that:

* creates and friendbot-funds wallets on **Stellar testnet**
* returns the current daily challenge prompt (text + hash)
* accepts replay payload and returns proof artifacts using Task 3 prover

### Endpoints (required)

1. `POST /wallet`

* generate Stellar keypair
* call friendbot to fund
* return `{ publicKey, secretKey }`
* Add big warning comment: demo-only

2. `GET /challenge/current`

* For now, backend can store prompt text in config and compute hash
* Return `{ challenge_id, prompt, prompt_hash_hex }`

3. `POST /prove`

* body: `{ challenge_id, player_pubkey, prompt, events_bytes_base64 }`
* normalize prompt identically to guest
* compute prompt hash
* call prover
* return:

  * `{ score, wpm_x100, accuracy_bps, duration_ms, image_id_hex, journal_sha256_hex, seal_hex }`

### Validation

* limit replay size (e.g. max 4096 events)
* reject prompts > 256 chars
* reject missing fields

### Tests

* Unit tests for:

  * prompt normalization and hash matches guest expectations
  * `/prove` returns values for a fixture replay
* For friendbot call:

  * mock in unit tests; add optional integration test behind env flag

### Commands you MUST run

* start backend and run these manual smoke tests (provide commands):

  * `curl -X POST /wallet`
  * `curl /challenge/current`
  * `curl -X POST /prove` with a fixture payload

### Output

Update README with:

* how to run backend
* expected responses